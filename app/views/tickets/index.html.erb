<p id="notice"><%= notice %></p>

<h1>Historial de Tickets</h1>

<table>
  <thead>
    <tr>
      <th>Cantidad</th>
      <th>Payment</th>
      <th>Estado</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tickets.each do |ticket| %>
      <tr>
        <td><%= ticket.cantidad %></td>
        <td><%= ticket.payment.id %></td>
        <td><%= ticket.status==1?"No aplicable":"aplicable"%></td>
        <td><%= link_to 'Mostrar', ticket %></td>
        <td><%= link_to 'Editar', edit_ticket_path(ticket) %></td>
        <td><%= link_to 'Cancelar', ticket_path(ticket,ticket:{:status => 1}), target: "_blank", :method=> :put  %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ticket', new_ticket_path %>
